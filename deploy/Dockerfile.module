# Dockerfile Module ETHAN (unifié)
FROM python:3.12-slim

WORKDIR /app

COPY core/ core/
COPY sdk/ sdk/
COPY pyproject.toml README.md ./

RUN pip install --no-cache-dir -e ".[server,dev]"

ENV PYTHONPATH=/app/core:/app/sdk:/app

CMD ["python", "-m", "core.modules.launcher"]