FROM postgres:16-alpine
RUN apk add --no-cache bash curl aws-cli
COPY deploy/postgres/backup/backup.sh /usr/local/bin/backup.sh
RUN chmod +x /usr/local/bin/backup.sh
CMD ["sh", "-c", "while true; do sleep 21600; /usr/local/bin/backup.sh; done"]
