#!/usr/bin/env python3
"""ETHAN — native Linux CLI for ETHAN Cognitive OS.

Usage:
  ethan <message>          One-shot: send a message and print response
  ethan chat               Interactive REPL conversation loop
  ethan status             System status (mode, goal, last event)
  ethan --help             Show this help

Environment:
  ETHAN_API=http://localhost:8000   Override API base URL
"""
import os
import sys

# Make `src/` importable regardless of where the script lives
_this = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, os.path.join(_this, "src"))

from cli import main

if __name__ == "__main__":
    main()